/** 这是fds */
export interface fds{
    /** 这是fds */
    fds: string,
    测试: Record<string, Record<string, number> | number>
}
/** 这是enum e1 */
export enum e1 {
    /** 这是enum nan */
    nan=10,
    nv=20
}
export default [];
