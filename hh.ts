import { e1, fds } from './fds';
interface hh{
    name:string
}

interface Int{
    age: number
}

/**
 *  @description 这是测试函数
 * @param as 这是参数1
 * @param sdf 这是参数2
 */
export const test = (as: hh, sdf:Int, s: e1, sd = new Date('2021-1-1')) =>{

};
